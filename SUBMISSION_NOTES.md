# Submission Notes

Project name: Orbit Tasks

Assignment: Full Stack Developer - React Native Predominant

What is included:

- React Native CLI TypeScript mobile app source
- Node.js + Express backend
- MongoDB task storage
- JWT email/password authentication
- Creative UI with dark product styling and colored priority states
- Bonus features: categories, filters, completion status, and priority/deadline sorting

Suggested submission description:

```text
I built Orbit Tasks, a React Native CLI TypeScript to-do app with a Node.js/Express and MongoDB backend. It supports registration/login, authenticated task storage, task creation with title/description/date-time/deadline/priority/category, completion toggling, deletion, filtering, and a priority + deadline scoring algorithm. I also focused on a creative visual interface with dark dashboard styling, priority color bands, compact status cards, and a smooth mobile-first layout.
```

Before submitting:

1. Push this folder to a GitHub repository.
2. Add screenshots or a short screen recording if possible.
3. Submit the repository link in the Google Form.

